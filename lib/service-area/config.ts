/** Geographic denies are off until hiring-area defaults and platform holds are re-enabled. */
export const SERVICE_AREA_ENFORCE_PLATFORM_HOLDS = false;
export const SERVICE_AREA_ENFORCE_HIRING_AREA = false;
