/** Supabase Storage bucket for worker onboarding uploads (see Storage → worker_required_files). */
export const WORKER_REQUIRED_FILES_BUCKET = "worker_required_files"

/** Resume files from application step 1 (see Storage → worker-resumes). */
export const WORKER_RESUMES_BUCKET = "worker-resumes"

/** Public tenant/organization logos for branded login and onboarding. */
export const ORGANIZATION_LOGOS_BUCKET = "organization-logos"
