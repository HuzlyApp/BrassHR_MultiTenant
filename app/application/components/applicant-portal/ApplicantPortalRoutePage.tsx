"use client";

import type { ReactNode } from "react";

/** Shell and session are provided by applicant-dashboard layout. */
export function ApplicantPortalRoutePage({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
