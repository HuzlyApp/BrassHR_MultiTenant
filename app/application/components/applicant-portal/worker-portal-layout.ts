/** Worker portal layout breakpoint — desktop sidebar + collapse from 1000px up. */
export const WORKER_PORTAL_DESKTOP_MIN = 1000;

export const WORKER_PORTAL_DESKTOP_MEDIA = `(min-width: ${WORKER_PORTAL_DESKTOP_MIN}px)`;
